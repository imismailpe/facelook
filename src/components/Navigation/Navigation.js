import React from 'react';

const Navigation = ()=>{
	return(
		<nav style={{display:'flex', justifyContent:'flex-end'}} >
			<p className="f3 dim link white underline pa2 ma1 pointer">Sign out</p>
		</nav>
		);
}

export default Navigation;